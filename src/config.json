{
  "comment": "Rules for the demo website",
  "default": false,
  "enforce-structure": {
    "structure": {
      "h1": {
        "p": "required",
        "h2": "required"
      },
      "h2": {
        "p": "required",
        "code": "required",
        "h3": "optional"
      },
      "h3": {
        "p": "required",
        "code": "optional"
      }
    }
  }
}
